# ─────────────────────────────────────────────────────────────────────────────
# Supabase (entorno de test aislado)
# ─────────────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# URL base para app/tests
NEXT_PUBLIC_SITE_URL=http://127.0.0.1:3000
PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000

# Seguridad de dominio (requeridas por backend)
UNSUBSCRIBE_TOKEN_SECRET=
TOKEN_ENCRYPTION_KEY=

# Integraciones externas (se mantienen como placeholders en modo mock)
GOOGLE_CLIENT_ID=e2e-mock-client-id
GOOGLE_CLIENT_SECRET=e2e-mock-client-secret
QSTASH_TOKEN=e2e-mock-qstash-token
QSTASH_CURRENT_SIGNING_KEY=e2e-mock-signing-key-current
QSTASH_NEXT_SIGNING_KEY=e2e-mock-signing-key-next

# Modo mock de externos para E2E
E2E_MOCK_EXTERNALS=true

# Credenciales del operador de pruebas
E2E_OPERATOR_EMAIL=operador.e2e@example.com
E2E_OPERATOR_PASSWORD=OperadorE2E!123
